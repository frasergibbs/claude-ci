# Copy to .github/workflows/claude-merge.yml in your repo.
# The workflow names in the `workflows` list must match your repo's workflow names.
name: Claude Merge

on:
  workflow_run:
    workflows: ["CI", "Claude Review"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write
  checks: read
  id-token: write

jobs:
  claude-merge:
    if: github.event.workflow_run.event == 'pull_request' && github.event.workflow_run.conclusion == 'success'
    uses: frasergibbs/claude-ci/.github/workflows/claude-merge.yml@v1
    with:
      OPS_BRIDGE_INTERNAL_URL: ${{ vars.OPS_BRIDGE_INTERNAL_URL }}
      OPS_BRIDGE_GITHUB_AUDIENCE: ${{ vars.OPS_BRIDGE_GITHUB_AUDIENCE }}
    secrets: inherit
